name: fast_package
description: "A lightweight Flutter toolkit with debounce/throttle, async queue, extensions, and UI widgets (Toast, Shimmer, gradient borders). Depends only on the Flutter SDK."
version: 0.0.5
homepage: https://arturoyi.github.io/fast_package/
repository: https://github.com/ArturoYi/fast_package
issue_tracker: https://github.com/ArturoYi/fast_package/issues
documentation: https://arturoyi.github.io/fast_package/
topics:
  - flutter
  - widget
  - ui
  - utilities
  - toast

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=3.19.6"

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.22.0

flutter:
