name: fast_package
description: 一个帮助您快速开发的Flutter 包，如果您发现任何需要改进的地方，或者想要请求某个功能，请在GitHub上提交issue，感谢您的反馈和贡献。
version: 0.0.2
homepage: https://github.com/ArturoYi/fast_package

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.22.0

flutter: