# Contributing

感谢你对 Fast Package 的关注！

- 代码与测试：在本地运行 `flutter test` 与 `flutter analyze`。
- 文档：文档站点位于 `docs/`（VitePress）。修改后请在 `docs/` 目录执行 `npm ci`、`npm run docs:dev` 或 `npm run docs:build` 验证。说明见 [docs/README.md](docs/README.md)。
- 请通过 GitHub Issue / Pull Request 提交 Bug、功能建议与文档改进。
